# Legacy Astro Tooling

This directory contains historical R&D scripts/models retained for reference and optional diagnostics.

- `cli/`: older comparison/generation scripts (v1-v4/Chebyshev/archive flows).
- `oracle/`: fixed-point approximation/parity models used during earlier engine iterations.

These are intentionally outside the active `astro` API surface.
